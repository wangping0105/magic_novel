<div class="container ">
  <nav class="">
    <ul class="pager">
      <li>
        <%= link_to '上一章', big_show_book_book_chapter_path(@book, prev_chapeter_by(20, @prev_book_chapter).prev_chapter_id) if prev_chapeter_by(20, @prev_book_chapter).prev_chapter_id.to_i != 0 %>
      </li>
      <li>
        <%= link_to '目录', book_path(@book)%>
      </li>
      <li>
        <%= link_to '下一章', big_show_book_book_chapter_path(@book, @book_chapter.next_chapter_id) if @book_chapter.next_chapter_id.to_i != 0 %>
      </li>
    </ul>
  </nav>
    <% @arr.each do |book_chapter| %>
      <div>
          <div class="book-header">
            <h1 class="book-title"><%= book_chapter.title %></h1>
          </div>
          <div class="book-main">
            <div class="col-sm-12 bg-white pad-5 book-content">
              <%=raw book_chapter.content %>
            </div>
          </div>
      </div>
    <%end %>
  <nav class="">
    <ul class="pager">
      <li>
        <%= link_to '上一章', big_show_book_book_chapter_path(@book,  prev_chapeter_by(20, @prev_book_chapter).prev_chapter_id) if prev_chapeter_by(20, @prev_book_chapter).prev_chapter_id.to_i != 0 %>
      </li>
      <li>
        <%= link_to '目录', book_path(@book)%>
      </li>
      <li>
        <%= link_to '下一章', big_show_book_book_chapter_path(@book, @book_chapter.next_chapter_id) if @book_chapter.next_chapter_id.to_i != 0 %>
      </li>
    </ul>
  </nav>

</div>


